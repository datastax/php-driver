---
Cassandra\FutureClose:
  comment: |-
    A future returned from Cassandra\Session::closeAsync().

    @see Session::closeAsync()
  methods:
    get:
      comment: |
        {@inheritDoc}

        @throws Exception\InvalidArgumentException
        @throws Exception\TimeoutException
      params:
        timeout:
          comment: ""
          type: float|null
      return:
        comment: a value that the future has been resolved with
        type: mixed
...
