---
FutureClose:
  comment: |-
    A future returned from Session::closeAsync().

    @see Session::closeAsync()
  methods:
    get:
      comment: ""
      params:
        timeout:
          comment: ""
          type: int|double|null
      return:
        comment: Nothing
        type: "null"
...
