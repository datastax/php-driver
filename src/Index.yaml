---
Index:
  comment: A PHP representation of an index
  methods:
    name:
      comment: Returns the name of the index
      return:
        comment: Name of the index
        type: string
    kind:
      comment: Returns the kind of index
      return:
        comment: Kind of the index
        type: string
    target:
      comment: Returns the target column of the index
      return:
        comment: Target column name of the index
        type: string
    option:
      comment: Return a column's option by name
      params:
        name:
          comment: The name of the option
          type: string
      return:
        comment: Value of an option by name
        type: \Cassandra\Value
    options:
      comment: Returns all the index's options
      return:
        comment: A dictionary of `string` and `Value` pairs of the index's options.
        type: array
    className:
      comment: Returns the class name of the index
      return:
        comment: Class name of a custom index
        type: string
    isCustom:
      comment: Determines if the index is a custom index.
      return:
        comment: true if a custom index
        type: bool
...
