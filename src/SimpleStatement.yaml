---
SimpleStatement:
  comment: |-
    Simple statements can be executed using a Session instance.
    They are constructed with a CQL string that can contain positional
    argument markers `?`.

    NOTE: Positional argument are only valid for native protocol v2+.

    @see Session::execute()
  methods:
    __construct:
      comment: Creates a new simple statement with the provided CQL.
      params:
        cql:
          comment: CQL string for this simple statement
          type: string
...
